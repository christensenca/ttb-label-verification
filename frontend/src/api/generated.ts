// Placeholder — replaced by `pnpm gen:api` once the backend is running.
// This stub exists so imports compile during early scaffold work.

export interface paths {}
export interface components {
  schemas: Record<string, unknown>
}
export type operations = Record<string, never>
